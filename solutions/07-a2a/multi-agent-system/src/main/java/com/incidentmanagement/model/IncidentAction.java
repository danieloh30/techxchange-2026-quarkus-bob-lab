package com.incidentmanagement.model;

/**
 * Enum representing the type of possible actions for incident processing
 */
public enum IncidentAction {
    ESCALATE,
    INVESTIGATE,
    TRIAGE,
    MONITOR,
    RESOLVE
}
